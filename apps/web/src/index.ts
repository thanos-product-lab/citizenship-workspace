// Placeholder so the package type-checks and lints cleanly on an empty scaffold.
// The Next.js App Router shell replaces this in M1 slice 5 (design tokens land
// in slice 4, consumed from @cw/design-system).
export const WEB_PLACEHOLDER = true as const;
