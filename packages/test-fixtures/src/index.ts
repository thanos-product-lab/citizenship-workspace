// Placeholder. Shared synthetic fixtures (FE + BE + evals) land alongside the
// canonical synthetic demo case in M3A. Synthetic data only — never real PII.
export const TEST_FIXTURES_PLACEHOLDER = true as const;
