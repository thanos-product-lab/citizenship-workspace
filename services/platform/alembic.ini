[alembic]
script_location = migrations
prepend_sys_path = .
version_path_separator = os

# The database URL is set at runtime in migrations/env.py from application
# settings, so it is intentionally not hard-coded here.
