"""The claim→fact path: proposals, the decisions about them, and the facts they become.

`values.py` is the file to read first. It carries the boundary prime directive 1 rests
on — a `FactVersion` can only be built from a `ReviewedValue`, and only a flushed
`ClaimReviewDecision` can produce one — as types rather than as rules.
"""
